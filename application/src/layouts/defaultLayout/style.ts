import styled from "styled-components";

export const DefalutLayoutContainer = styled.div`
    background-color: ${props => props.theme["gray-800"]};
    max-width: 70rem;
    height: calc(100vh - 10rem);
    margin: 4.62rem auto 0 auto;
    border-radius: 8px;
    padding: 2.5rem;
;
  

`